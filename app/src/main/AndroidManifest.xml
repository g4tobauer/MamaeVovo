<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.desenvolvigames.mamaevovo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.PrincipalActivitty" />
        <activity android:name=".activities.ProductActivity" />
        <activity android:name=".activities.ProductListActivity" />
        <activity
            android:name=".activities.SalesOrderActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".activities.SalesOrderItemActivity" />
        <activity
            android:name=".activities.TesteActivity"
            android:label="@string/title_activity_teste"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.SubItemActivity" />
        <activity android:name=".activities.SubItemListActivity" />
        <activity android:name=".activities.CadastreBaseActivity" />
        <activity android:name=".activities.SalesOrderListActivity" />
        <activity android:name=".activities.ReportsActivity"></activity>
    </application>

</manifest>